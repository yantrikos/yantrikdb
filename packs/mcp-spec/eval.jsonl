{"id": "no-initialize", "q": "In MCP specification revision 2026-07-28, what replaced the initialize/notifications-initialized handshake for conveying protocol version and client capabilities?", "expect": [["_meta"], ["protocolVersion", "protocol version"]], "note": "The handshake is gone; every request carries io.modelcontextprotocol/protocolVersion and clientCapabilities in _meta."}
{"id": "no-session", "q": "What happened to the Mcp-Session-Id header in MCP revision 2026-07-28, and how does a server keep state that spans multiple calls now?", "expect": [["removed", "gone", "ignore", "no longer"], ["handle", "tool argument", "argument"]], "note": "Sessions are removed; state travels as explicit server-minted handles in ordinary tool arguments."}
{"id": "resource-404-code", "q": "What JSON-RPC error code does a 2026-07-28 MCP server return for a resource that is not found, and which older code must it no longer emit?", "expect": [["-32602"], ["-32002"]], "note": "Changed to align with JSON-RPC Invalid Params; -32002 is retired but accepted from older servers."}
{"id": "header-mismatch-code", "q": "Which error code and name does an MCP server use when an Mcp-Name header does not match the request body value?", "expect": [["-32020"], ["HeaderMismatch"]], "note": "400 Bad Request + HeaderMismatch, renumbered from -32001."}
{"id": "unsupported-version", "q": "What error does an MCP server return for a protocol version it does not implement, and what does its data field list so the client can retry?", "expect": [["-32022", "UnsupportedProtocolVersion"], ["supported"]], "note": "data.supported lists versions; client retries with a mutual one."}
{"id": "mrtr-carrier", "q": "In MCP 2026-07-28, how does a server deliver an elicitation/create or sampling/createMessage request to the client, given that server-initiated JSON-RPC requests are no longer allowed?", "expect": [["InputRequiredResult", "input_required"], ["inputRequests"]], "note": "MRTR: the server returns resultType input_required with an inputRequests map; the client retries with inputResponses."}
{"id": "request-state-client", "q": "What must an MCP client do with the requestState string it receives in an InputRequiredResult?", "expect": [["echo", "exact", "unchanged", "untouched", "same value"], ["not inspect", "not parse", "not modify", "opaque", "never inspect"]], "note": "Echo it back exactly on retry; it is opaque and must never be inspected or modified."}
{"id": "mrtr-which-requests", "q": "Which three client request methods are the only ones allowed to receive an input_required result in MCP 2026-07-28?", "expect": [["tools/call"], ["resources/read"], ["prompts/get"]], "note": "Servers MUST NOT send InputRequiredResult on any other request."}
{"id": "subscribe-replacement", "q": "Which single method replaced resources/subscribe, resources/unsubscribe and the HTTP GET notification stream in MCP 2026-07-28?", "expect": [["subscriptions/listen"]], "note": "One long-lived request whose response stream carries opted-in notifications."}
{"id": "sub-ack-first", "q": "What must be the first message a server sends on a new MCP subscription stream?", "expect": [["acknowledged"]], "note": "notifications/subscriptions/acknowledged, carrying the honored filter subset, before any other notification for that subscription."}
{"id": "sub-id-value", "q": "What value does io.modelcontextprotocol/subscriptionId carry on notifications from a listen stream?", "expect": [["JSON-RPC id", "request id", "id of the", "listen request"]], "note": "It is the JSON-RPC id of the originating subscriptions/listen request — used to demultiplex on stdio."}
{"id": "required-headers", "q": "Name the HTTP headers that every MCP Streamable HTTP POST request must include to mirror the JSON-RPC body for intermediaries.", "expect": [["MCP-Protocol-Version"], ["Mcp-Method"]], "note": "Plus Mcp-Name on tools/call, resources/read, prompts/get."}
{"id": "param-header", "q": "When a tool parameter is annotated with x-mcp-header: \"Region\", what HTTP header name does the client send its value under?", "expect": [["Mcp-Param-Region", "Mcp-Param"]], "note": "x-mcp-header builds Mcp-Param-{name}; clients MUST support it, and invalid annotations exclude the tool from tools/list."}
{"id": "b64-sentinel", "q": "How does an MCP client carry a non-ASCII value in an Mcp-Param or Mcp-Name header?", "expect": [["=?base64?", "base64"]], "note": "The sentinel format =?base64?...?= of the UTF-8 bytes, lowercase markers."}
{"id": "http-cancel", "q": "How does an MCP client cancel an in-flight request on the Streamable HTTP transport in 2026-07-28?", "expect": [["closing", "close", "closes", "closed"], ["stream", "connection"]], "note": "Closing the SSE response stream IS the cancellation signal; notifications/cancelled is stdio-only."}
{"id": "deprecated-trio", "q": "Which three MCP features were deprecated together in revision 2026-07-28?", "expect": [["Roots"], ["Sampling"], ["Logging"]], "note": "SEP-2577: remain functional in the window, but new implementations should not add them."}
{"id": "logging-migration", "q": "With the MCP Logging feature deprecated, how is per-request log level requested, and where should stdio servers log instead?", "expect": [["logLevel"], ["stderr", "OpenTelemetry"]], "note": "io.modelcontextprotocol/logLevel in _meta; never emit notifications/message for requests without it."}
{"id": "cacheable-fields", "q": "Which two fields must list-type MCP results such as tools/list carry to support client caching, per the CacheableResult interface?", "expect": [["ttlMs"], ["cacheScope"]], "note": "ttlMs freshness hint; cacheScope public/private for shared intermediaries."}
{"id": "discover-method", "q": "Which RPC must every MCP 2026-07-28 server implement so clients can learn its supported protocol versions and capabilities up front?", "expect": [["server/discover"]], "note": "Also the backward-compatibility probe on stdio."}
{"id": "sse-resume", "q": "What must an MCP client do when an SSE response stream breaks mid-request, given that Last-Event-ID resumability was removed?", "expect": [["re-issue", "reissue", "new request", "retry"], ["new", "different"]], "note": "The in-flight request is lost; re-issue as a new request with a new request id."}
{"id": "dcr-replacement", "q": "What replaced OAuth Dynamic Client Registration as the preferred MCP client registration mechanism in 2026-07-28?", "expect": [["Client ID Metadata Document"]], "note": "RFC 7591 DCR is deprecated but kept for authorization servers without metadata-document support."}
{"id": "retry-id", "q": "When an MCP client retries a request after an input_required result, may it reuse the original JSON-RPC id?", "expect": [["must not", "different", "new id", "new JSON-RPC", "cannot"]], "note": "The retry is an independent request and MUST use a different id."}
