{"id": "string-templates", "q": "The Java string template syntax written as STR.\"...\" appeared in preview. What happened to it, and in which JDK version?", "expect": [["withdraw", "withdrawn", "removed", "dropped", "abandoned"], ["23"]], "note": "Previewed in 21 and 22, withdrawn in 23. Models emit it confidently because the preview is in their training data. Asking for the version stops a vague 'it changed' from passing.", "short_ok": ["23"]}
{"id": "compact-constructor", "q": "Inside a Java record's compact constructor, why is writing this.hi = x a compile error, and what do you assign instead?", "expect": [["parameter", "parameters", "hi = ", "implicit", "assigns the field", "after"]], "note": "The canonical constructor assigns fields from the parameters after the compact block runs."}
{"id": "record-shallow", "q": "A Java record has a List component. What depth of immutability does the record actually provide, and what must the compact constructor do about it?", "expect": [["shallow", "shallowly", "not deep", "only the reference", "still mutable", "reference"], ["copyof", "defensive", "unmodifiable", "copy"]], "note": "Records are shallowly immutable; List.copyOf both copies and rejects nulls."}
{"id": "switch-guard-keyword", "q": "Complete this Java 21 pattern switch case label - which single keyword goes in the blank? case Circle c ___ c.radius() > 10 ->", "expect": [["when"]], "note": "It is `when`, not `if`. Models write `if`. Phrased as a fill-in-the-blank so the answer must name the keyword rather than describe guards in general.", "short_ok": ["when"]}
{"id": "switch-null", "q": "What happens when a traditional Java switch on a reference type receives null, and how does a pattern switch let you handle it?", "expect": [["nullpointerexception", "npe", "throws"], ["case null"]], "note": "Pattern switches can take `case null ->` explicitly."}
{"id": "sealed-permits", "q": "In Java, what three modifiers may a class that is permitted by a sealed interface declare itself as?", "expect": [["final"], ["sealed"], ["non-sealed"]], "note": "Exhaustiveness depends on the hierarchy staying closed."}
{"id": "virtual-thread-pool", "q": "Why is putting Java virtual threads behind a fixed-size thread pool self-defeating?", "expect": [["cheap", "inexpensive", "no cost", "not expensive", "amortis", "amortise", "amortiz", "creation cost"], ["limit", "reimpose", "reimposes", "defeat", "defeating", "cap", "constrain", "bottleneck", "point of", "purpose"]], "note": "Pooling exists to amortise platform-thread creation cost that virtual threads do not have, so a pool reimposes the limit it was meant to remove."}
{"id": "pinning", "q": "In Java 21, which language construct pins a virtual thread to its carrier, and what was the recommended alternative?", "expect": [["synchronized", "monitor"], ["reentrantlock"]], "note": "JEP 491 in Java 24 removed the synchronized pinning; on 21 the advice stands."}
{"id": "executor-closeable", "q": "Since which Java version can an ExecutorService be used in a try-with-resources block, and what happens if a non-daemon pool is never shut down?", "expect": [["19", "nineteen"], ["jvm", "keeps running", "doesn't exit", "does not exit", "hang", "alive", "never terminates", "won't exit"]], "note": "The 'my program will not exit' bug that looks like a deadlock. 19 is a genuine short answer.", "short_ok": ["19"]}
{"id": "sequenced-collections", "q": "Which Java 21 interface added getFirst(), getLast() and reversed() to List and Deque?", "expect": [["sequencedcollection", "sequenced collection"]], "note": "Retrofitted onto the existing collection types."}
{"id": "dcl-volatile", "q": "In Java double-checked locking, what goes wrong if the singleton field is not declared volatile?", "expect": [["partially constructed", "partially initialized", "partially initialised", "not fully", "incompletely", "reorder", "half-built", "uninitialized", "uninitialised"]], "note": "The reference can be published before the constructor's writes are visible."}
{"id": "chm-atomic", "q": "Why is if (!map.containsKey(k)) map.put(k, v) still a race condition on a Java ConcurrentHashMap, and which methods fix it?", "expect": [["two operation", "not atomic", "between", "compound", "check-then-act", "check then act", "race"], ["computeifabsent", "putifabsent", "merge"]], "note": "Individually thread-safe calls do not compose."}
{"id": "interrupted-flag", "q": "What must a Java catch block for InterruptedException do if it does not rethrow?", "expect": [["interrupt()", "restore", "reassert", "set the flag", "currentthread().interrupt"]], "note": "Swallowing it is why shutdown hangs."}
{"id": "bigdecimal-double", "q": "Why does new BigDecimal(0.1) in Java not equal 0.1, and which two constructions do give exactly 0.1?", "expect": [["binary", "double", "not exactly", "representation", "floating"], ["valueof"], ["string", "\"0.1\"", "0.1\""]], "note": "The double was never 0.1 to begin with."}
{"id": "bigdecimal-equals", "q": "Java's BigDecimal has both equals and compareTo. What does equals compare that compareTo does not, and which should be used to test numeric equality?", "expect": [["scale"], ["compareto"]], "note": "new BigDecimal(\"1.0\").equals(new BigDecimal(\"1.00\")) is false. Asking WHAT differs rules out a lucky \"use compareTo\"."}
{"id": "integer-cache", "q": "Why does comparing two boxed Java Integers with == appear to work for small values but fail for large ones?", "expect": [["cache", "caches", "cached"], ["-128", "127", "128"]], "note": "Integer.valueOf caches -128..127, so tests pass and production fails."}
{"id": "simpledateformat", "q": "What makes java.text.SimpleDateFormat unsafe to share as a static field across threads, and which java.time class replaces it?", "expect": [["mutable", "internal state", "not thread-safe", "not threadsafe", "shared state", "stateful"], ["datetimeformatter"]], "note": "It holds mutable parse state and produces wrong dates rather than exceptions."}
{"id": "collectors-tomap", "q": "What does Java's Collectors.toMap do when two elements produce the same key, and how do you handle it?", "expect": [["illegalstateexception", "throws", "exception"], ["merge"]], "note": "The two-arg form throws; supply a merge function."}
{"id": "stream-tolist", "q": "What is the difference in mutability between stream.toList() and stream.collect(Collectors.toList()) in Java?", "expect": [["unmodifiable", "immutable", "cannot be modified", "can't be modified"], ["mutable", "modifiable", "arraylist"]], "note": "A refactor between them surfaces as UnsupportedOperationException."}
{"id": "xxe-default", "q": "Java's DocumentBuilderFactory resolves something dangerous by default. What is it, and which feature string disables it outright?", "expect": [["external entit", "doctype", "xxe", "external general"], ["disallow-doctype-decl"]], "note": "External entity resolution is on out of the box: file disclosure and SSRF."}
{"id": "securerandom", "q": "Why must java.util.Random not be used to generate session tokens in Java, and what should be used?", "expect": [["predict", "recover", "linear congruential", "lcg", "48-bit", "48 bit", "guess", "deterministic", "seed"], ["securerandom"]], "note": "The full future output follows from two consecutive values."}
{"id": "twr-suppressed", "q": "In Java try-with-resources, if both the body and close() throw, which exception propagates and where does the other one go?", "expect": [["body", "try block", "original", "primary", "first"], ["suppress", "getsuppressed"]], "note": "A hand-written finally inverts this and loses the real cause."}
