{"id": "listof-immutable", "q": "What happens when you call add() on a list created with List.of() in Java, and what must you do if you need a mutable copy?", "expect": [["UnsupportedOperationException"], ["ArrayList"]], "note": "of-factories return immutable collections; wrap in new ArrayList<>()."}
{"id": "concat-loop", "q": "Why is appending to a String with += inside a Java loop a performance problem, and which class fixes it?", "expect": [["quadratic", "O(n", "copy", "immutable"], ["StringBuilder"]], "note": "Strings are immutable; the loop is quadratic."}
{"id": "read-string", "q": "Which java.nio.file.Files method reads an entire text file into a String in one call?", "expect": [["readString"]], "note": "Models invent Files.readLines or reach for BufferedReader; readString exists since 11."}
{"id": "path-factory", "q": "What is the modern factory method for creating a java.nio.file.Path, replacing Paths.get?", "expect": [["Path.of"]], "note": "Paths.get is the legacy spelling."}
{"id": "virtual-threads", "q": "Which Executors factory method creates an executor that runs each task on its own virtual thread?", "expect": [["newVirtualThreadPerTaskExecutor"]], "note": "The Java 21+ answer for I/O-bound concurrency."}
{"id": "http-send", "q": "On java.net.http.HttpClient, which method sends a request synchronously, and what second argument chooses how the response body is read?", "expect": [["send", "sends"], ["BodyHandler", "bodyhandlers"]], "note": "client.send(request, HttpResponse.BodyHandlers.ofString())."}
{"id": "getfirst-version", "q": "Since which Java version can you call getFirst() directly on a List, and which interface introduced it?", "expect": [["Java 21", "version 21", "JDK 21"], ["SequencedCollection"]], "note": "Calling it on 17 is the invented-API failure this pack targets."}
{"id": "optional-fallback", "q": "Which Optional method returns the contained value or a supplied default when empty, without throwing?", "expect": [["orElse"]], "note": "orElse / orElseGet, not get()."}
{"id": "grouping", "q": "Which Collectors method groups stream elements into a Map keyed by a classifier function?", "expect": [["groupingBy"]], "note": "The standard grouping collector."}
{"id": "date-pattern", "q": "Which class and method create a custom date pattern like dd/MM/yyyy for parsing a LocalDate in Java?", "expect": [["DateTimeFormatter"], ["ofPattern"]], "note": "LocalDate.parse(text, DateTimeFormatter.ofPattern(...))."}
{"id": "instant-millis", "q": "Which Instant factory method converts epoch milliseconds into an Instant?", "expect": [["ofEpochMilli"]], "note": "Not fromMillis or valueOf — ofEpochMilli."}
{"id": "then-compose", "q": "On CompletableFuture, which method chains a step that itself returns a CompletableFuture, avoiding a nested future?", "expect": [["thenCompose"]], "note": "thenApply nests; thenCompose flattens."}
{"id": "chm-nulls", "q": "What does ConcurrentHashMap do when you put a null key or null value?", "expect": [["NullPointerException", "not allow", "does not permit", "no null", "rejects"]], "note": "Unlike HashMap, nulls are rejected."}
{"id": "latch-wait", "q": "Which CountDownLatch method blocks the calling thread until the count reaches zero?", "expect": [["await"]], "note": "countDown decrements; await blocks."}
{"id": "regex-flow", "q": "In java.util.regex, which two calls turn a pattern string into per-input match state you can call find() on?", "expect": [["compile"], ["matcher"]], "note": "Pattern.compile(...).matcher(input)."}
{"id": "token-random", "q": "Which class should generate session tokens or salts in Java, and why not java.util.Random?", "expect": [["SecureRandom"], ["predictab", "cryptograph", "security", "seed"]], "note": "Random is predictable; SecureRandom is cryptographically strong."}
{"id": "sha256", "q": "Which MessageDigest call obtains a SHA-256 hasher in Java?", "expect": [["getInstance"], ["SHA-256"]], "note": "MessageDigest.getInstance(\"SHA-256\")."}
{"id": "b64-url", "q": "Which Base64 method returns the URL-safe encoder in Java?", "expect": [["getUrlEncoder"]], "note": "getEncoder uses +/ which break in URLs."}
{"id": "process-exit", "q": "After starting an external process with ProcessBuilder, which Process method blocks until it finishes and returns the exit code?", "expect": [["waitFor"]], "note": "start() launches; waitFor() joins."}
{"id": "interrupt-restore", "q": "When catching InterruptedException in Java, what must the catch block do besides handling it, to avoid swallowing the interrupt?", "expect": [["interrupt"]], "note": "Thread.currentThread().interrupt() — restore the flag."}
