{"id": "forwardref-19", "q": "In React 19, how does a function component receive a ref, and what is the status of forwardRef?", "expect": [["prop", "props"], ["deprecated", "not required", "no longer", "unnecessary", "not needed"]], "note": "ref is an ordinary prop in 19; forwardRef is deprecated."}
{"id": "use-hook", "q": "What does the React 19 use() hook do, and in what way are its calling rules different from useContext?", "expect": [["promise", "suspend", "context"], ["conditionally", "conditional", "inside a condition", "in a loop", "inside loops", "loops"]], "note": "use() may be called conditionally and in loops, unlike every other hook."}
{"id": "use-promise-cache", "q": "Why does calling use() on a promise created during render in React cause the component to suspend forever?", "expect": [["new promise", "new one", "each render", "every render", "recreated", "different promise", "never the same", "re-created"]], "note": "Each render creates a fresh promise, so it never resolves from React's perspective."}
{"id": "useactionstate", "q": "Name the three values returned by React 19's useActionState hook.", "expect": [["state"], ["action", "formaction", "dispatch"], ["pending", "ispending"]], "note": "[state, formAction, isPending]"}
{"id": "useformstatus-location", "q": "React's useFormStatus() returns pending state for a form. From which component must it be called?", "expect": [["inside", "child", "descendant", "within the form", "under the form", "nested"]], "note": "Calling it in the component that renders the form returns nothing useful."}
{"id": "useoptimistic", "q": "Which React 19 hook shows an intended result immediately and reverts automatically if the action fails?", "expect": [["useoptimistic"]], "note": "Replaces the hand-rolled set-then-roll-back pattern."}
{"id": "context-provider-19", "q": "In React 19, what shorter syntax replaces <MyContext.Provider value={x}>?", "expect": [["<mycontext value", "context value=", "without .provider", "no .provider", "drop the provider", "omit .provider", "directly as", "as the provider"]], "note": "<MyContext value={x}> — .Provider is deprecated."}
{"id": "strictmode-double", "q": "Why does React 18+ run every effect twice on mount in development, and what does it mean if a component breaks under it?", "expect": [["strictmode", "strict mode", "remount", "unmount", "deliberate", "intentional", "on purpose", "test"], ["cleanup", "clean up", "missing cleanup", "bug", "incorrect"]], "note": "The right fix is the cleanup, never a hasRun ref."}
{"id": "ref-callback-cleanup", "q": "What can a React 19 ref callback return that it could not before, and what did React previously do to signal detachment?", "expect": [["cleanup", "clean-up", "teardown", "disconnect function", "destructor"], ["null"]], "note": "It used to be called a second time with null; returning a cleanup is now the documented shape."}
{"id": "index-key", "q": "Under exactly what conditions is using the array index as a React key acceptable?", "expect": [["append", "never reorder", "no reorder", "static", "stable", "not reordered", "no id", "never change", "doesn't change", "end of the list", "no filter", "no delet"]], "note": "Models say 'avoid it' without knowing when it is fine."}
{"id": "key-remount", "q": "In React, what happens to a component's internal state when its key prop changes, and what pattern does this replace?", "expect": [["reset", "remount", "unmount", "discarded", "destroyed", "new instance", "fresh"], ["effect", "useeffect", "reset state", "componentwillreceiveprops", "syncing", "derived"]], "note": "It replaces the reset-state-in-an-effect pattern and is cheaper and synchronous."}
{"id": "usestate-initializer", "q": "What is the difference in React between useState(createInitialValue()) and useState(createInitialValue)?", "expect": [["every render", "each render", "every time", "on each render", "runs each", "called each", "always runs", "re-runs"], ["once", "only once", "initial", "first render", "lazy"]], "note": "The first calls the function on every render and throws away the result."}
{"id": "falsy-zero", "q": "What does React render for {items.length && <List />} when items is an empty array, and why?", "expect": [["0", "zero"], ["falsy", "renders numbers", "number", "returns 0", "evaluates to 0", "short-circuit"]], "note": "React renders the number 0; only false, null and undefined render nothing. 0 is a genuine short answer.", "short_ok": ["0"]}
{"id": "error-boundary-limits", "q": "Name two categories of errors that a React error boundary does NOT catch.", "expect": [["event handler", "handler"], ["async", "settimeout", "promise", "callback", "asynchronous"]], "note": "Boundaries only cover render, lifecycle and constructors."}
{"id": "error-boundary-class", "q": "React error boundaries cannot be written as function components. Which kind of component is required, and which two methods implement one?", "expect": [["class"], ["getderivedstatefromerror"], ["componentdidcatch"]], "note": "Neither method has a hook equivalent, which is why this one construct stayed a class."}
{"id": "context-rerender", "q": "In React, if a context value is an object literal, which consumers re-render when it changes, and what are the two remedies?", "expect": [["all", "every", "each"], ["memo", "usememo"], ["split", "separate", "multiple context", "several context"]], "note": "There is no per-field subscription; consumers re-render regardless of what they read."}
{"id": "nested-component", "q": "Why must a React component never be defined inside another component's render body?", "expect": [["new type", "new function", "different type", "recreated", "new component", "identity", "every render", "each render"], ["remount", "unmount", "loses state", "lose state", "state is lost", "reset", "destroy"]], "note": "The subtree unmounts and remounts on every parent render."}
{"id": "rsc-limits", "q": "Name two things a React Server Component cannot do.", "expect": [["hook", "usestate", "state", "useeffect", "effect"], ["event handler", "onclick", "interactiv", "browser event"]], "note": "They never hydrate, so there is no client runtime for them."}
{"id": "use-client-placement", "q": "In React, should the 'use client' directive go on the page component or on the leaf component that needs interactivity, and why?", "expect": [["leaf", "component that needs", "as low", "lowest", "deepest", "boundary", "smallest"], ["bundle", "javascript", "ship", "client", "size", "everything imported", "below"]], "note": "Everything imported below the boundary enters the bundle."}
{"id": "react19-removed", "q": "Name two APIs removed in React 19 that older React code commonly used: one for declaring prop types or defaults, and one for mounting the app.", "expect": [["proptypes", "defaultprops"], ["reactdom.render", "reactdom.hydrate", "createroot", "react-dom/client"]], "note": "Two DISJOINT groups on purpose — identical alternatives in both groups would let one match satisfy a question that asked for two things."}
{"id": "href-xss", "q": "JSX escapes interpolated text, so what are the two remaining ways a React component can introduce XSS?", "expect": [["dangerouslysetinnerhtml"], ["href", "javascript:", "url", "src", "scheme", "protocol"]], "note": "javascript: URLs in href are the one models consistently miss."}
{"id": "fetch-race", "q": "Two fetches fire from a React effect as a user types, and the first resolves last. What is the resulting bug and what is the standard fix?", "expect": [["stale", "old", "wrong", "outdated", "previous", "earlier", "race"], ["abortcontroller", "abort", "cleanup", "ignore", "cancel", "flag"]], "note": "Cleanup runs before the next effect, which is what makes the guard work."}
