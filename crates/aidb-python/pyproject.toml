[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "aidb-rust"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "aidb._aidb_rust"
python-source = "../../src"
