# Trace contract T02 — versioned, release-blocking once implemented.
# Transitions: pending -> implemented (with implemented_since + test_path).
# implemented -> pending is ILLEGAL (registry test + CI base-diff enforce).
contract_version = 1
id = "T02"
title = "chain-head-resolution"
item = "1"           # v0.10 plan item that must make this passable
owner = "yantrikdb-core"
status = "pending"
# implemented_since = ""    # set on implementation (version string)
# test_path = ""            # set on implementation (module::test_fn)
seeds = "YANTRIKDB_HNSW_SEED via testing feature; fixtures declare exact vectors"
clock = "injectable via testing feature (set_test_clock); no wall-clock asserts"

fixture = '''
Supersedes chain A->B->C plus branch/cycle fixtures (replication-merged).
'''

assertions = [
  "resolve_current(any member) = (C, status)",
  "recall leads with C",
  "no cycles accepted at local write",
  "branch fixtures resolve deterministically via the fold",
]
