# Trace contract T13 — versioned, release-blocking once implemented.
# Transitions: pending -> implemented (with implemented_since + test_path).
# implemented -> pending is ILLEGAL (registry test + CI base-diff enforce).
contract_version = 1
id = "T13"
title = "fitted-defaults-regression"
item = "2"           # v0.10 plan item that must make this passable
owner = "yantrikdb-core"
status = "pending"
# implemented_since = ""    # set on implementation (version string)
# test_path = ""            # set on implementation (module::test_fn)
seeds = "YANTRIKDB_HNSW_SEED via testing feature; fixtures declare exact vectors"
clock = "injectable via testing feature (set_test_clock); no wall-clock asserts"

fixture = '''
Learned ranking weights proposed as new shipped defaults (v0.11 sealed protocol).
'''

assertions = [
  "frozen query-level split + seed",
  "sealed comparator/version/artifact",
  "primary metric + minimum margin declared ahead",
  "per-category and worst-query non-regression floors",
  "one-shot holdout evaluation",
  "fitted defaults ship only if they beat shipped heuristics on HELD-OUT queries",
]
