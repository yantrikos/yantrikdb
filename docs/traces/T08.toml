# Trace contract T08 — versioned, release-blocking once implemented.
# Transitions: pending -> implemented (with implemented_since + test_path).
# implemented -> pending is ILLEGAL (registry test + CI base-diff enforce).
contract_version = 1
id = "T08"
title = "absence-with-coverage"
item = "1b"           # v0.10 plan item that must make this passable
owner = "yantrikdb-core"
status = "pending"
# implemented_since = ""    # set on implementation (version string)
# test_path = ""            # set on implementation (module::test_fn)
seeds = "YANTRIKDB_HNSW_SEED via testing feature; fixtures declare exact vectors"
clock = "injectable via testing feature (set_test_clock); no wall-clock asserts"

fixture = '''
Query a scope with zero relevant records; and one with only below-threshold candidates.
'''

assertions = [
  "typed SearchCoverage: namespaces searched, kinds searched, threshold tau",
  "no-matching-record vs below-threshold distinguished typed, not prose",
]
